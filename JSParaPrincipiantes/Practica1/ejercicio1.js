// a. Corrige el siguiente código
let nombre = "Armando"; // Usamos let porque el nombre se reasigna más adelante
const edad = 25; // Usamos const porque la edad no cambia

nombre = "Ana Maria"; // Se reasigna el valor de nombre [cite: 16]

// Se crea el saludo usando los valores actuales de las variables
const saludo = `Hola, ${nombre}. Tienes ${edad} años.`; // Usamos ` (template literals) para un código más limpio


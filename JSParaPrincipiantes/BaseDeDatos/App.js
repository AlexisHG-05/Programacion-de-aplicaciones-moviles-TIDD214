import React from 'react';
// Importamos la pantalla que creaste en la carpeta screens
import InsertUsuarioScreen from './screens/InsertUsuarioScreen';

export default function App() {
  // Renderizamos directamente tu pantalla de práctica
  return (
    <InsertUsuarioScreen />
  );
}